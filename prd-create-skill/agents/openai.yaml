interface:
  display_name: "PRD Create Skill"
  short_description: "从原型图扫描、确认并生成通用 Markdown PRD"
  default_prompt: "Use $prd-create-skill to scan these prototypes, confirm blocking decisions, and create a portable Markdown PRD."
